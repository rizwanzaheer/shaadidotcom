// import { fromJS } from 'immutable';
// import { selectEditProfileContainerDomain } from '../selectors';

describe('selectEditProfileContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
